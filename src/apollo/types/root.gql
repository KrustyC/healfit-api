directive @auth(role: Role = USER) on FIELD_DEFINITION

enum Role {
  ADMIN
  USER
}

type ValueObject {
  id: Int
  name: String
}

type InitialData {
  ingridientsCategories: [ValueObject]
}

type Query {
  init: InitialData
}

type Mutation {
  hello: String
}